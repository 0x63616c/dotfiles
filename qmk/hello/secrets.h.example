#pragma once
// Template. Copy to secrets.h and fill in real values:
//   cp secrets.h.example secrets.h
// secrets.h is gitignored — real passwords never hit the repo.
#define SECRET_PW1 "your-first-password"
#define SECRET_PW2 "your-second-password"
